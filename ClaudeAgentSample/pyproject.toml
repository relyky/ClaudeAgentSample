[project]
name = "claudeagentsample"
version = "0.1.0"
description = "Claude Agent SDK Interactive CLI Application"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "claude-agent-sdk==0.1.7",
    "python-dotenv==1.2.1",
]
