[project]
name = "local-mcp-sample"
version = "0.1.0"
description = "Local MCP Server Development Samples"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "aiohttp>=3.13.2",
    "mcp[cli]>=1.21.2",
]
